# 🔧 Исправление таблицы Homework в Supabase

## 🔴 Проблема

При добавлении домашнего задания на деплоенном сайте (Render.com) возникает ошибка.

**Возможные причины:**
1. Таблица `homework` не существует в Supabase
2. Таблица имеет неправильную структуру колонок
3. Row Level Security (RLS) блокирует операции INSERT/DELETE

---

## ✅ РЕШЕНИЕ (Выполните ЭТИ ШАГИ ПРЯМО СЕЙЧАС!)

### Шаг 1: Откройте Supabase SQL Editor

1. Перейдите на https://supabase.com
2. Войдите в аккаунт
3. Выберите ваш проект **jqfifytmlpofxzwkeavm**
4. В левом меню нажмите **SQL Editor**
5. Нажмите **New query** (или используйте прямую ссылку ниже)

**Прямая ссылка:**
```
https://supabase.com/dashboard/project/jqfifytmlpofxzwkeavm/sql/new
```

---

### Шаг 2: Создайте таблицу homework

**Скопируйте и выполните этот SQL:**

```sql
-- ========================================
-- СОЗДАНИЕ ТАБЛИЦЫ HOMEWORK
-- ========================================

-- Удалить таблицу, если существует (ОСТОРОЖНО: удалит все данные!)
DROP TABLE IF EXISTS homework CASCADE;

-- Создать таблицу homework с правильной структурой
CREATE TABLE homework (
  id BIGSERIAL PRIMARY KEY,
  title TEXT NOT NULL,
  description TEXT NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- ========================================
-- ОТКЛЮЧЕНИЕ ROW LEVEL SECURITY
-- ========================================
-- Для быстрого тестирования отключаем RLS
ALTER TABLE homework DISABLE ROW LEVEL SECURITY;

-- ========================================
-- ПРОВЕРКА
-- ========================================
-- Проверяем, что таблица создана
SELECT * FROM homework;
```

**Нажмите кнопку Run** (или Ctrl+Enter)

**Ожидаемый результат:**
```
Success. No rows returned
```

Это означает, что таблица создана успешно, но пока пуста.

---

### Шаг 3 (Альтернатива): Если хотите оставить RLS включённым

Вместо отключения RLS можете создать policy:

```sql
-- Включить RLS
ALTER TABLE homework ENABLE ROW LEVEL SECURITY;

-- Создать policy для публичного доступа
CREATE POLICY "Allow public access to homework"
ON homework
FOR ALL
TO anon
USING (true)
WITH CHECK (true);
```

⚠️ **Внимание:** Это разрешит ВСЕМ добавлять/удалять задания через API.  
Для production рекомендуется настроить более строгие policies.

---

## 🧪 Тестирование после исправления

### Локальное тестирование (http://localhost:5173)

1. **Войдите как модератор:**
   - Пароль: **`N3PqH8M6`** (Бектур - Moderator)
   
2. **Перейдите на страницу "Домашние задания"**

3. **Добавьте тестовое задание:**
   - Название: `Тест`
   - Описание: `Проверка работы`
   - Нажмите **Добавить задание**

4. **Проверьте логи backend** (PowerShell окно):
   ```
   ============================================================
   ADDING HOMEWORK TO SUPABASE
   ============================================================
   Data received: {'title': 'Тест', 'description': 'Проверка работы'}
   Title: Тест
   Description: Проверка работы
   SUCCESS: Homework added with ID: 1
   ============================================================
   ```

5. **Задание должно появиться в списке!**

---

### Тестирование на деплоенном сайте (Render.com)

1. **После выполнения SQL в Supabase откройте ваш сайт:**
   ```
   https://your-app.onrender.com
   ```

2. **Войдите как модератор** (пароль `N3PqH8M6`)

3. **Попробуйте добавить задание**

4. **Проверьте логи на Render:**
   - Перейдите в Dashboard → Logs
   - Должны увидеть "SUCCESS: Homework added"

---

## 📊 Проверка через Supabase Table Editor

Вы можете проверить добавленные задания прямо в Supabase:

1. Откройте **Table Editor** в Supabase
2. Выберите таблицу **homework**
3. Вы увидите все добавленные задания

**Прямая ссылка:**
```
https://supabase.com/dashboard/project/jqfifytmlpofxzwkeavm/editor/public/homework
```

---

## 📋 Структура таблицы homework

| Колонка | Тип | Описание |
|---------|-----|----------|
| `id` | BIGSERIAL (auto-increment) | Уникальный ID задания |
| `title` | TEXT (NOT NULL) | Название задания |
| `description` | TEXT (NOT NULL) | Описание задания |
| `created_at` | TIMESTAMPTZ | Дата создания (автоматически) |

---

## ❓ Частые ошибки и решения

### ❌ Ошибка: "Could not find the 'title' column"
**Причина:** Таблица не создана или имеет неправильную структуру  
**Решение:** Выполните SQL из Шага 2

### ❌ Ошибка: "new row violates row-level security policy"
**Причина:** RLS блокирует операцию INSERT  
**Решение:** Выполните `ALTER TABLE homework DISABLE ROW LEVEL SECURITY;`

### ❌ Ошибка: "relation 'homework' does not exist"
**Причина:** Таблица вообще не существует в Supabase  
**Решение:** Выполните SQL из Шага 2

### ❌ Задание добавляется, но не отображается
**Причина:** Ошибка в GET запросе или frontend  
**Решение:** Проверьте логи backend, обновите страницу (F5)

---

## 🎯 Чеклист после исправления

После выполнения всех шагов проверьте:

- [ ] ✅ Таблица `homework` создана в Supabase
- [ ] ✅ RLS отключён или настроены policies
- [ ] ✅ Модераторы могут добавлять ДЗ (локально)
- [ ] ✅ Студенты видят список заданий
- [ ] ✅ Модераторы могут удалять задания
- [ ] ✅ Всё работает на деплоенном сайте (Render)

---

## 🚀 Итого

**Вам нужно выполнить ТОЛЬКО ЭТО:**

1. 🔓 Откройте Supabase SQL Editor
2. 📝 Скопируйте и выполните SQL из Шага 2
3. ✅ Готово! Homework работает на всех окружениях

**Никаких изменений в коде не требуется!**  
Backend уже настроен и готов к работе.

---

## ⚠️ Безопасность

**Текущая настройка (RLS отключён):**
- ✅ Подходит для тестирования
- ⚠️ НЕ рекомендуется для production
- 🔒 Любой может добавлять/удалять через API

**Рекомендация для production:**
- Используйте policy из Шага 3
- Или добавьте проверку роли в backend
- Или используйте service_role ключ (не на frontend!)

---

**Если возникнут проблемы:**
1. Проверьте логи backend (PowerShell или Render Logs)
2. Проверьте Table Editor в Supabase
3. Убедитесь, что RLS отключён для таблицы homework
