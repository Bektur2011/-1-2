# 🔧 Исправление таблицы Homework в Supabase

## 🔴 Проблема

При добавлении домашнего задания возникает ошибка:
```
Could not find the 'title' column of 'homework' in the schema cache
```

Это означает, что таблица `homework` либо:
- Не существует в Supabase
- Имеет неправильную структуру колонок

---

## ✅ РЕШЕНИЕ

### Шаг 1: Создать таблицу homework с правильной структурой

1. Откройте **Supabase SQL Editor**:
   ```
   https://supabase.com/dashboard/project/jqfifytmlpofxzwkeavm/sql/new
   ```

2. Вставьте и выполните следующий SQL:

```sql
-- Удалить таблицу, если существует
DROP TABLE IF EXISTS homework CASCADE;

-- Создать таблицу homework
CREATE TABLE homework (
  id BIGSERIAL PRIMARY KEY,
  title TEXT NOT NULL,
  description TEXT NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Отключить Row Level Security (для тестирования)
ALTER TABLE homework DISABLE ROW LEVEL SECURITY;

-- Или создать policy для публичного доступа:
-- CREATE POLICY "Allow public access"
-- ON homework
-- FOR ALL
-- TO anon
-- USING (true)
-- WITH CHECK (true);
```

3. Нажмите **Run** (или Ctrl+Enter)

---

### Шаг 2: Проверить, что таблица создана

Выполните запрос для проверки:

```sql
SELECT * FROM homework;
```

Должен вернуться пустой результат (пустая таблица).

---

### Шаг 3: Проверить добавление ДЗ

Попробуйте добавить тестовое задание через frontend:
1. Откройте http://localhost:5173
2. Войдите с паролем модератора: **`N3PqH8M6`** (Бектур - Moderator)
3. Перейдите на страницу "Домашние задания"
4. Добавьте тестовое задание

---

## 📋 Структура таблицы homework

| Колонка | Тип | Описание |
|---------|-----|----------|
| `id` | BIGSERIAL (auto-increment) | Уникальный ID задания |
| `title` | TEXT (NOT NULL) | Название задания |
| `description` | TEXT (NOT NULL) | Описание задания |
| `created_at` | TIMESTAMPTZ | Дата создания (автоматически) |

---

## 🎯 После исправления

1. ✅ Таблица `homework` создана с правильной структурой
2. ✅ RLS отключён или настроены policies
3. ✅ Модераторы и админы могут добавлять ДЗ
4. ✅ Студенты могут видеть список заданий
5. ✅ Модераторы и админы могут удалять задания

---

## 🚀 Для Production (на Render)

После локального тестирования:
1. Выполните тот же SQL в Supabase (для production)
2. Приложение на Render автоматически начнёт работать с новой таблицей
3. Никаких изменений в коде не требуется!

---

## ⚠️ Важно

- Row Level Security (RLS) **отключён** для простоты
- Для production рекомендуется настроить policies для безопасности
- Backend использует `anon` ключ, поэтому нужны соответствующие права
