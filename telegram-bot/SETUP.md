# 🚀 Пошаговая инструкция по настройке

## Шаг 1: Создание таблицы в Supabase

1. Откройте ваш проект в [Supabase](https://supabase.com)
2. Перейдите в **SQL Editor** (боковое меню)
3. Нажмите **New Query**
4. Вставьте этот SQL-код:

```sql
-- Создание таблицы
CREATE TABLE homework_bot (
  id BIGSERIAL PRIMARY KEY,
  title TEXT NOT NULL,
  description TEXT NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Включение Row Level Security
ALTER TABLE homework_bot ENABLE ROW LEVEL SECURITY;

-- Политика для чтения (публичный доступ)
CREATE POLICY "Публичное чтение" ON homework_bot
  FOR SELECT
  USING (true);

-- Политика для вставки
CREATE POLICY "Запись через сервис" ON homework_bot
  FOR INSERT
  WITH CHECK (true);
```

5. Нажмите **Run** или **F5**
6. Должно появиться сообщение "Success. No rows returned"

## Шаг 2: Получение ключей Supabase

1. В Supabase перейдите в **Settings** → **API**
2. Найдите и скопируйте:
   - **Project URL** (например: `https://abcdefgh.supabase.co`)
   - **service_role key** (длинный ключ, нажмите "Reveal" чтобы увидеть)
   - **anon public key** (для веб-страницы)

⚠️ **Важно:** `service_role` key держите в секрете! Не публикуйте его в GitHub.

## Шаг 3: Создание Telegram-бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду: `/newbot`
3. Введите имя бота (например: `StudyCore Bot`)
4. Введите username бота (должен заканчиваться на `bot`, например: `studycore_homework_bot`)
5. BotFather пришлет токен вида: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`
6. **Скопируйте этот токен!**

## Шаг 4: Получение вашего Telegram ID

1. Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
2. Бот автоматически пришлет ваш ID (например: `123456789`)
3. Запишите ID всех администраторов, которые будут использовать бота

## Шаг 5: Установка зависимостей

```bash
# Перейдите в папку telegram-bot
cd telegram-bot

# Установите зависимости
npm install
```

Должны установиться:
- node-telegram-bot-api
- @supabase/supabase-js
- dotenv
- nodemon (для разработки)

## Шаг 6: Настройка переменных окружения

1. Скопируйте файл `.env.example` в `.env`:
```bash
cp .env.example .env
```

2. Откройте `.env` в текстовом редакторе:
```bash
# Windows
notepad .env

# Linux/Mac
nano .env
```

3. Заполните данные:
```env
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
SUPABASE_URL=https://abcdefgh.supabase.co
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
ADMIN_IDS=123456789,987654321
```

**Пояснения:**
- `BOT_TOKEN` - токен от BotFather
- `SUPABASE_URL` - Project URL из Supabase
- `SUPABASE_KEY` - **service_role key** из Supabase (не anon!)
- `ADMIN_IDS` - Telegram ID администраторов через запятую

4. Сохраните файл

## Шаг 7: Запуск бота

```bash
# Запуск
npm start
```

Вы должны увидеть:
```
🤖 Бот запущен и готов к работе!
👥 Администраторы: 123456789, 987654321
```

**Бот готов!** Теперь можно отправлять команды в Telegram.

## Шаг 8: Настройка веб-страницы

1. Откройте `public/index.html` в редакторе
2. Найдите строки (около 215):
```javascript
const SUPABASE_URL = 'YOUR_SUPABASE_URL';
const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';
```

3. Замените на ваши данные из Supabase:
```javascript
const SUPABASE_URL = 'https://abcdefgh.supabase.co';
const SUPABASE_ANON_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...';
```

⚠️ **Важно:** Здесь используйте **anon public key**, а не service_role!

4. Сохраните файл

## Шаг 9: Открытие веб-страницы

Просто откройте файл `public/index.html` в браузере (двойной клик) или:

```bash
# Если установлен Python 3
cd public
python -m http.server 8000

# Откройте в браузере: http://localhost:8000
```

## ✅ Проверка работы

### Проверка бота:

1. Найдите вашего бота в Telegram по username
2. Отправьте `/start` - должно прийти приветствие
3. Отправьте `/add Тест | Это тестовое задание`
4. Должно прийти подтверждение с ID задания

### Проверка веб-страницы:

1. Откройте `index.html` в браузере
2. Должно загрузиться тестовое задание
3. При добавлении нового задания через бота, оно должно появиться автоматически

## 🎉 Готово!

Теперь вы можете:
- Добавлять задания через бота командой `/add`
- Просматривать все задания командой `/list`
- Видеть все задания на веб-странице
- Автоматически обновлять веб-страницу при добавлении новых заданий

## 🆘 Частые проблемы

### "У вас нет доступа к этому боту"
- Проверьте ваш ID у [@userinfobot](https://t.me/userinfobot)
- Убедитесь, что ID добавлен в `ADMIN_IDS` в `.env`
- Перезапустите бота: `Ctrl+C`, затем `npm start`

### "Ошибка при добавлении задания"
- Проверьте `SUPABASE_KEY` в `.env` (должен быть service_role key)
- Убедитесь, что таблица `homework_bot` создана в Supabase
- Проверьте политики RLS в Supabase

### Веб-страница не загружается
- Откройте консоль браузера (F12)
- Проверьте `SUPABASE_URL` и `SUPABASE_ANON_KEY` в `index.html`
- Убедитесь, что политика "Публичное чтение" создана

### Бот не запускается
- Проверьте `.env` файл (должен быть в папке `telegram-bot`)
- Убедитесь, что все пакеты установлены: `npm install`
- Проверьте токен бота у [@BotFather](https://t.me/BotFather)
