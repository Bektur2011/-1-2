# StudyCore Telegram Bot 🤖

Telegram-бот для администраторов школы, который упрощает работу с домашними заданиями.

## 📋 Возможности

- ✅ Добавление домашних заданий через команду `/add`
- 📝 Просмотр всех заданий через команду `/list`
- 🔐 Доступ только для администраторов
- 💾 Автоматическое сохранение в Supabase
- 🌐 Веб-интерфейс для просмотра заданий
- ⚡ Автоматическое обновление данных в реальном времени

## 🚀 Быстрый старт

### 1. Создание таблицы в Supabase

Выполните этот SQL-запрос в редакторе SQL Supabase:

```sql
CREATE TABLE homework_bot (
  id BIGSERIAL PRIMARY KEY,
  title TEXT NOT NULL,
  description TEXT NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Включить Row Level Security (опционально, если нужны ограничения доступа)
ALTER TABLE homework_bot ENABLE ROW LEVEL SECURITY;

-- Политика для чтения (публичный доступ для веб-страницы)
CREATE POLICY "Публичное чтение" ON homework_bot
  FOR SELECT
  USING (true);

-- Политика для записи (только через service_role key)
CREATE POLICY "Запись через сервис" ON homework_bot
  FOR INSERT
  WITH CHECK (true);
```

### 2. Создание Telegram-бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Придумайте имя и username для бота
4. Скопируйте токен бота

### 3. Получение Telegram ID

1. Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
2. Бот автоматически пришлет ваш ID
3. Запишите ID всех администраторов

### 4. Установка и настройка

```bash
# Перейти в папку с ботом
cd telegram-bot

# Установить зависимости
npm install

# Скопировать файл окружения
cp .env.example .env

# Отредактировать .env (вставить свои данные)
nano .env
```

Содержимое `.env`:
```env
BOT_TOKEN=ваш_токен_бота
SUPABASE_URL=https://ваш-проект.supabase.co
SUPABASE_KEY=ваш_service_role_key
ADMIN_IDS=123456789,987654321
```

**Важно:** Используйте `service_role` key из Supabase (Settings → API → service_role), а не `anon` key!

### 5. Запуск бота

```bash
# Запуск в production
npm start

# Запуск в development (с автоперезагрузкой)
npm run dev
```

## 📱 Использование бота

### Команды

#### `/start`
Приветственное сообщение и список команд

#### `/add заголовок | описание`
Добавить новое домашнее задание

**Пример:**
```
/add Математика | Решить задачи №1-10 на странице 45
```

#### `/list`
Показать все домашние задания с датами создания

#### `/help`
Справка по использованию бота

## 🌐 Веб-интерфейс

### Настройка

1. Откройте файл `public/index.html`
2. Найдите строки:
```javascript
const SUPABASE_URL = 'YOUR_SUPABASE_URL';
const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';
```
3. Замените на свои данные из Supabase (Settings → API):
   - `SUPABASE_URL` - Project URL
   - `SUPABASE_ANON_KEY` - anon public key

### Запуск веб-страницы

Просто откройте файл `public/index.html` в браузере или разместите на хостинге:

```bash
# Простой HTTP сервер (если установлен Python)
cd public
python -m http.server 8000

# Или используйте любой другой веб-сервер
```

### Возможности веб-интерфейса

- ✅ Автоматическое отображение всех заданий
- ⚡ Обновление в реальном времени при добавлении через бота
- 📱 Адаптивный дизайн для мобильных устройств
- 🎨 Красивый градиентный интерфейс

## 🔧 Структура проекта

```
telegram-bot/
├── bot.js              # Основной код бота
├── supabase.js         # Подключение к Supabase
├── package.json        # Зависимости проекта
├── .env.example        # Пример файла окружения
├── .gitignore         # Игнорируемые файлы
├── README.md          # Документация
└── public/
    └── index.html     # Веб-интерфейс
```

## 🛠 Технологии

- **Node.js** - серверная платформа
- **node-telegram-bot-api** - работа с Telegram Bot API
- **@supabase/supabase-js** - работа с базой данных Supabase
- **dotenv** - управление переменными окружения

## 📝 Примечания

- Бот работает только с пользователями из списка `ADMIN_IDS`
- Все остальные пользователи получат сообщение об отсутствии доступа
- Дата создания задания заполняется автоматически
- Веб-страница обновляется в реальном времени через Supabase Realtime

## 🐛 Решение проблем

### Бот не отвечает
- Проверьте правильность токена бота в `.env`
- Убедитесь, что бот запущен (`npm start`)
- Проверьте интернет-соединение

### Ошибка подключения к Supabase
- Проверьте `SUPABASE_URL` и `SUPABASE_KEY` в `.env`
- Убедитесь, что таблица `homework_bot` создана
- Проверьте права доступа (RLS policies)

### Веб-страница не показывает данные
- Проверьте настройки в `index.html` (URL и anon key)
- Откройте консоль браузера (F12) для просмотра ошибок
- Убедитесь, что политики RLS настроены правильно

### "У вас нет доступа к этому боту"
- Проверьте свой Telegram ID у [@userinfobot](https://t.me/userinfobot)
- Убедитесь, что ваш ID добавлен в `ADMIN_IDS` в `.env`
- Перезапустите бота после изменения `.env`

## 📄 Лицензия

MIT
