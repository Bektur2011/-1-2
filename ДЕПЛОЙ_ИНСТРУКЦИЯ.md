# 🚀 Деплой StudyCore с Telegram ботом

## Что изменено

Теперь при деплое **автоматически запускаются**:
1. ✅ **Flask сервер** (сайт) на порту из переменной `PORT`
2. ✅ **Telegram бот** (работает постоянно в фоне)

## Файлы конфигурации

### 1. `start_services.py` (новый файл)
Главный скрипт запуска, который:
- Запускает Flask приложение
- Запускает Telegram бота
- Управляет обоими процессами одновременно
- Корректно завершает оба сервиса при остановке

### 2. `build.sh` (обновлён)
Теперь также устанавливает зависимости для бота:
```bash
npm ci  # в папке telegram-bot/
```

### 3. `Procfile` (обновлён)
```
web: python start_services.py
```

### 4. `nixpacks.toml` (обновлён)
```toml
[start]
cmd = "python start_services.py"
```

## Переменные окружения для деплоя

### Для сайта (Flask):
```env
PORT=5000                    # Устанавливается автоматически платформой
SUPABASE_URL=https://...     # Из StudyCore/.env
SUPABASE_KEY=...             # Из StudyCore/.env
GEMINI_API_KEY=...           # Из StudyCore/.env (для AI чата)
```

### Для бота (Telegram):
```env
BOT_TOKEN=...                # Из telegram-bot/.env
ADMIN_IDS=123,456            # Из telegram-bot/.env
```

**ВАЖНО:** Убедитесь что на платформе деплоя (Render/Railway) добавлены ВСЕ эти переменные!

## Как задеплоить

### На Render.com:

1. **Подключите репозиторий** к Render
2. **Выберите тип**: Web Service
3. **Build Command**: `./build.sh`
4. **Start Command**: `python start_services.py`
5. **Добавьте Environment Variables**:
   - `SUPABASE_URL`
   - `SUPABASE_KEY`
   - `GEMINI_API_KEY`
   - `BOT_TOKEN`
   - `ADMIN_IDS`

### На Railway.app:

1. **Подключите репозиторий** к Railway
2. Railway автоматически определит `nixpacks.toml`
3. **Добавьте Environment Variables** (те же что выше)
4. **Deploy!**

## Проверка работы

После деплоя в логах вы должны увидеть:

```
🚀 STUDYCORE - Запуск всех сервисов
============================================================
Сервисы:
  1. Flask Backend + Frontend (порт 5000)
  2. Telegram Bot
============================================================
[FLASK] Server started on port 5000
[BOT] 🤖 Бот запущен и готов к работе!
[BOT] 👥 Администраторы: 123, 456
✅ Все сервисы запущены!
```

## Локальный запуск (для тестирования)

```bash
# Убедитесь что зависимости установлены
pip install -r StudyCore/backend/requirements.txt
cd telegram-bot && npm install && cd ..

# Запустите оба сервиса
python start_services.py
```

## Устранение проблем

### Бот не запускается
1. Проверьте что файл `telegram-bot/.env` существует
2. Проверьте что `BOT_TOKEN` и `ADMIN_IDS` указаны в переменных окружения на платформе
3. Посмотрите логи: строки с `[BOT]` покажут ошибки бота

### Сайт не работает
1. Проверьте строки с `[FLASK]` в логах
2. Убедитесь что `SUPABASE_URL` и `SUPABASE_KEY` указаны

### Оба сервиса падают
1. Проверьте что `build.sh` выполнился успешно
2. Убедитесь что Node.js доступен на платформе (nixpacks.toml это настраивает)

## Архитектура

```
┌─────────────────────────────────────┐
│     start_services.py               │
│  (Главный процесс-менеджер)         │
└──────────┬──────────────┬───────────┘
           │              │
     ┌─────▼─────┐  ┌────▼──────┐
     │  Flask    │  │ Telegram  │
     │  Backend  │  │   Bot     │
     │  (Python) │  │ (Node.js) │
     └───────────┘  └───────────┘
           │              │
           ▼              ▼
      Supabase DB    Telegram API
```

## Важные заметки

- ✅ Бот работает **постоянно** (не нужно перезапускать)
- ✅ При падении одного сервиса второй продолжит работу
- ✅ Логи обоих сервисов видны в одном месте
- ✅ Корректное завершение при остановке контейнера
